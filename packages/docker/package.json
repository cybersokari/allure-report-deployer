{
  "name": "@allure/docker",
  "version": "1.0.0",
  "description": "Deploy your Allure Test Reports to a website, No server required",
  "scripts": {
    "test": "",
    "build": "rm -rf ./dist && tsc"
  },
  "type": "module",
  "dependencies": {
    "allure-deployer-shared": "file:../shared"
  },
  "private": true,
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "esbuild": "^0.24.1"
  }
}
