{
  "name": "allure-report-deployer",
  "version": "1.0.0",
  "workspaces": [
    "packages/shared",
    "packages/docker",
    "packages/action",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces"
  },
  "overrides": {
    "whatwg-url": "^14.0.0"
  }
}
