{
  "name": "allure-report-deployer",
  "version": "1.0.0",
  "workspaces": [
    "packages/shared",
    "packages/docker",
    "packages/action",
    "packages/cli"
  ],
  "scripts": {
    "build": "yarn workspaces run build"
  },
  "overrides": {
    "whatwg-url": "^14.0.0"
  }
}
