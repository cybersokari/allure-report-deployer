{
  "name": "allure-report-deployer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/docker",
    "packages/action"
  ],
  "scripts": {
    "build": "yarn workspaces run build"
  }
}